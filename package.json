{
  "name": "abstract-scanner",
  "version": "1.1.0",
  "description": "Universal scanner for lexical analysis.",
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "scripts": {
    "compile": "tsc src/*.ts -d --outDir lib",
    "lint": "tslint src/*.ts",
    "build": "npm run lint && npm run compile && webpack",
    "clean": "rm lib/*",
    "prepublish": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lemoi/abstract-scanner.git"
  },
  "author": "Wyatt",
  "keywords": [
    "vpp"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lemoi/abstract-scanner/issues"
  },
  "homepage": "https://github.com/lemoi/abstract-scanner#readme",
  "devDependencies": {
    "tslint": "^4.4.2",
    "typescript": "^2.2.1",
    "webpack": "^2.2.1"
  }
}
